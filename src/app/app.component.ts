//importer les elements importantes
import { Component } from '@angular/core';

@Component({
  // le selecteur donne un nom à notre composant
  selector: 'app-root',
  // la template contient le code html du composant
  templateUrl: 'app.component.html'
})
export class AppComponent {
 
}
